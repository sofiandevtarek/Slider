const slider = document.querySelector(".slidee");
const dots = document.querySelector(".dots");
let currentIndex = 0;

function showSlide(index) {
  slider.array.forEach((element) => {
    slider.classList.remove("active");
    dots[i].classList.remove("active");
  });
}
